{
  "name": "zingit-client",
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch",
    "deploy": "node scripts/prepare-deploy.js && gh-pages -d deploy"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.11",
    "gh-pages": "^6.3.0",
    "happy-dom": "^20.11.12",
    "terser": "^5.51.2",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
